spring:
  sql:
    init:
      mode: never
  datasource:
    url: jdbc:h2:mem:testdb
    username:
  h2:
    console:
      enable: true
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
